# ShopSense Product Analytics — Python dependencies
# Install with: pip install -r requirements.txt

# ── Database ───────────────────────────────────────────────
sqlalchemy>=2.0.0,<3.0.0        # ORM + engine used by all pipeline scripts
mysql-connector-python>=8.0.0   # MySQL dialect for SQLAlchemy (mysql+mysqlconnector://)

# ── Data loading & manipulation ────────────────────────────
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0

# ── Statistics — A/B test power & significance ─────────────
statsmodels>=0.14.0,<1.0.0

# ── Visualization ───────────────────────────────────────────
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# ── Notebooks ────────────────────────────────────────────────
jupyter>=1.0.0
